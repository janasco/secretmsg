{
  "name": "secretmsg",
  "version": "1.0.0",
  "description": "SecretMsg - Open Source Anonymous Messaging & Story Sticker Platform",
  "author": "janasco <janasco@duck.com>",
  "license": "MIT",
  "homepage": "https://secretmsg.net",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "preview": "npm --prefix apps/web run preview",
    "android:sync": "npm --prefix apps/mobile-android run sync",
    "android:build": "npm --prefix apps/mobile-android run build:apk",
    "android:open": "npm --prefix apps/mobile-android run open"
  }
}
